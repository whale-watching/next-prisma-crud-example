{
  "name": "nextjs-prisma-crud-example",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "prisma:studio": "dotenv -e .env.local -- npx prisma studio",
    "prisma:introspect": "dotenv -e .env.local -- npx prisma introspect",
    "prisma:generate": "dotenv -e .env.local -- npx prisma generate",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^2.10.1",
    "dotenv": "^8.2.0",
    "next": "10.0.0",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "@prisma/cli": "^2.10.1"
  }
}
